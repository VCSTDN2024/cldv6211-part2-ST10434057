# Ignore the .vs folder (Visual Studio's local settings and temporary files)
.vs/

# Ignore the build results (bin and obj directories)
bin/
obj/

# Ignore user-specific files (e.g., Visual Studio user settings)
*.user
*.userosscache
*.suo
*.sln.docstates

# Ignore the temporary files that may be generated by IDEs or tools
*.vspscc
*.vssscc

# Ignore ReSharper settings (if using ReSharper)
_ReSharper.Caches/

# Ignore the Visual Studio Code workspace settings (if using VS Code)
.vscode/

# Ignore database files (if you’re using SQL Server or SQLite)
*.mdf
*.ldf
*.sqlite

# Ignore npm and package manager folders (if working with Node.js or npm)
node_modules/

# Ignore the package-lock.json file (if you don't want to track your dependencies)
package-lock.json
